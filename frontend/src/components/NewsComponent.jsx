import React from 'react';

/**
 * NewsComponent - Utility component for API data debugging
 * Not currently used in the main UI, but kept for reference
 */
const NewsComponent = () => {
  return (
    <div style={{ display: 'none' }}>
      {/* Debug component - raw data view removed from production UI */}
    </div>
  );
};

export default NewsComponent;